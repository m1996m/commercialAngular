import { TypeProduitModel } from './type-produit-model.Model';

describe('TypeProduitModel', () => {
  it('should create an instance', () => {
    expect(new TypeProduitModel()).toBeTruthy();
  });
});
