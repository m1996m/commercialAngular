import { ProduitVenduModel } from './produit-vendu-model.Model';

describe('ProduitVenduModel', () => {
  it('should create an instance', () => {
    expect(new ProduitVenduModel()).toBeTruthy();
  });
});
